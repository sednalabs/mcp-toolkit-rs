[package]
name = "mcp-toolkit-process"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Process and signal helpers for Rust MCP services."
repository = "https://github.com/sednalabs/mcp-rs-toolkit"
readme = "../../README.md"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
libc = "0.2.180"
