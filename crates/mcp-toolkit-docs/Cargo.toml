[package]
name = "mcp-toolkit-docs"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Documentation and tool metadata helpers for Rust MCP projects."
repository = "https://github.com/sednalabs/mcp-rs-toolkit"
readme = "../../README.md"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
